name: "Get Top Ten Deals"

on: 
  schedule: 
      - cron: "30 2 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with: 
          python-version: "3.10"
      - run: "python -m QantasWineBonusPointsLister"
